{
  "name": "hello-linux",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:check": "python3 scripts/check_links.py",
    "docs:test": "python3 -m pytest tests/ -q",
    "docs:cov": "python3 -m pytest tests/ -q --cov=check_links --cov-report=term-missing"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
